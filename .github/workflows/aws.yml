on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

name: purbeurre CI on ECR

jobs:

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.7]


    steps:
    - name: Checkout
      uses: actions/checkout@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r app/requirements.txt

    - name: Create .env.prod file
      run: |
        touch .env.prod
        echo DEBUG=${{ secrets.DEBUG }} >> .env.prod
        echo SECRET_KEY=${{ secrets.SECRET_KEY }} >> .env.prod
        echo DJANGO_ALLOWED_HOSTS=${{ secrets.DJANGO_ALLOWED_HOSTS }} >> .env.prod
        echo SQL_ENGINE=${{ secrets.SQL_ENGINE }} >> .env.prod
        echo SQL_DATABASE=${{ secrets.SQL_DATABASE }} >> .env.prod
        echo SQL_USER=${{ secrets.SQL_USER }} >> .env.prod
        echo SQL_PASSWORD=${{ secrets.SQL_PASSWORD }} >> .env.prod
        echo SQL_HOST=${{ secrets.SQL_HOST }} >> .env.prod
        echo DATABASE=${{ secrets.DATABASE }} >> .env.prod
        echo VIRTUAL_HOST=${{ secrets.VIRTUAL_HOST }} >> .env.prod
        echo VIRTUAL_PORT=${{ secrets.VIRTUAL_PORT }} >> .env.prod
        echo LETSENCRYPT_HOST=${{ secrets.LETSENCRYPT_HOST }} >> .env.prod
        echo license_key = ${{ secrets.NEW_RELIC_LICENSE_KEY }} >> newrelic.ini

    - name: Create .env.prod.proxy-companion
      run: |
        touch .env.prod.proxy-companion
        echo DEFAULT_EMAIL=${{ secrets.DEFAULT_EMAIL }} >> .env.prod.proxy-companion
        echo NGINX_PROXY_CONTAINER=${{ secrets.NGINX_PROXY_CONTAINER }} >> .env.prod.proxy-companion

    - name: Create newrelic.ini file
      env:
        NEW_RELIC_APP_NAME: ${{ secrets.NEW_RELIC_APP_NAME }}
        NEW_RELIC_LICENSE_KEY: ${{ secrets.NEW_RELIC_LICENSE_KEY }}
      run: |
        touch new_relic/newrelic.ini
        echo "[newrelic]" >> newrelic/newrelic.in
        echo "distributed_tracing.enabled = true" >> newrelic/newrelic.in
        echo "monitor_mode = true" >> newrelic/newrelic.in
        echo "log_level = info" >> newrelic/newrelic.in
        echo "ssl = true" >> newrelic/newrelic.in
        echo "high_security = false" >> newrelic/newrelic.in
        echo "transaction_tracer.enabled = true" >> newrelic/newrelic.in
        echo "transaction_tracer.transaction_threshold = apdex_f" >> newrelic/newrelic.in
        echo "transaction_tracer.record_sql = obfuscated" >> newrelic/newrelic.in
        echo "transaction_tracer.stack_trace_threshold = 0.5" >> newrelic/newrelic.in
        echo "transaction_tracer.explain_enabled = true" >> newrelic/newrelic.in
        echo "transaction_tracer.explain_threshold = 0.5" >> newrelic/newrelic.in
        echo "transaction_tracer.function_trace =" >> newrelic/newrelic.in
        echo "error_collector.enabled = true" >> newrelic/newrelic.in
        echo "error_collector.ignore_errors =" >> newrelic/newrelic.in
        echo "browser_monitoring.auto_instrument = true" >> newrelic/newrelic.in
        echo "thread_profiler.enabled = true" >> newrelic/newrelic.in

    - name: Run Tests
      env:
        SECRET_KEY: ${{ secrets.SECRET_KEY }}
      run: |
        cd app
        python manage.py test

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: eu-west-3

    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1

    - name: Build, tag, and push the image to Amazon ECR
      id: build-image
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        ECR_REPOSITORY: ${{ secrets.REPO_NAME }}
        IMAGE_TAG: 1
      run: |
        # Build a docker container and push it to ECR 
        docker-compose -f docker-compose.prod.yml build
        echo "Pushing image to ECR..."
        docker push $ECR_REGISTRY/$ECR_REPOSITORY:web
        echo "::set-output name=image::$ECR_REGISTRY/$ECR_REPOSITORY:web"
        docker push $ECR_REGISTRY/$ECR_REPOSITORY:nginx-proxy
        echo "::set-output name=image::$ECR_REGISTRY/$ECR_REPOSITORY:nginx:proxy"